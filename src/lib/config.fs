module GovUk.Config

// ------------------------------------------------------------------------------------------------
// Global configuration for the data importer (private keys are in '../config/keys.fs')
// ------------------------------------------------------------------------------------------------

let container = "govuk"
let requestTimeout = 5000
let storageAccount = "https://thegammadata.blob.core.windows.net"
